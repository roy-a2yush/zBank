<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= page %>></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/simple-line-icons/2.4.1/css/simple-line-icons.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/addProducts.css">
</head>
<body>
    <div class="registration-form">
        <form action="<%= submitUrl %>" method="POST" enctype="multipart/form-data">
            <div class="form-icon">
                <span><i class="icon <%= icon %>"></i></span>
            </div>
            <% if (docs) { %>
                <p>Please change only those values you want to change, keep rest as it is, i.e., don't erase any feilds</p>
            <% } else {%>
                <p>Total number of accounts = <%= numProducts %></p>
            <% } %>
            <div class="form-group">
                <% if (docs) { %> <label for="username">Account Name</label> <% } %>
                <input type="text" class="form-control item" id="password" placeholder="Account Name" <% if (docs) { %> value="<%= docs.productName %>" <% } %> name="productName" required>
            </div>
            <div class="form-group">
                <% if (docs) { %> <label for="username">Account description</label> <% } %>
                <input type="text" class="form-control item" id="birth-date" placeholder="Account Description" <% if (docs) { %> value="<%= docs.brand %>" <% } %> name="brand">
            </div>
            <div class="container form-group">
                <div class="row justify-content-around">
                  <div class="col-4">
                    <button type="submit" class="btn btn-success" style="margin-right: 0.1rem; margin-left: 0.1rem;">Create Account</button>
                  </div>
                  <div class="col-4">
                    <a href="/"><button type="button" class="btn btn-danger" style="margin-right: 0.1rem; margin-left: 0.1rem;">Goto Dashboard</button></a>
                  </div>
                </div>
              </div>
        </form>
        <!-- <div style="align-self: center; clear: left; position: relative; padding-top: 2.5rem;"> -->
            <!-- <div>
                <a href="/product/dashboard"><button type="button" class="btn btn-success" style="margin-right: 0.1rem; margin-left: 0.1rem;">Goto Dashboard</button></a>
                <a href="/login/logout"><button type="button" class="btn btn-danger" style="margin-right: 0.1rem; margin-left: 0.1rem;">Logout</button></a>
            </div> -->
          <!-- </div> -->
    </div>
    <script type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.15/jquery.mask.min.js"></script>
</body>
</html>